// Note: Main AppLayOut component...!

import React, { FC } from 'react';
import TodoList from './todo/todo';

const AppLayOut: FC = () => {
  return (
    <TodoList />
  );
};

export default AppLayOut;